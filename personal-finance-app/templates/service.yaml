apiVersion: v1
kind: Service
metadata:
  name: {{ include "personal-finance-app.fullname" . }}
  labels:
    {{- include "personal-finance-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "personal-finance-app.selectorLabels" . | nindent 4 }}
